import { CollectionConfig } from "payload/types";
import { RowLabelArgs } from "payload/dist/admin/components/forms/RowLabel/types";


const LinkAff: CollectionConfig = {
    slug: "linkAff",
    fields: [
        
    ]
};

export default LinkAff